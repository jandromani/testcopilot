name: Run Pipeline after Build

on:
  workflow_run:
    workflows: ["Build and Push Docker Image"]
    types:
      - completed

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/jandromani/testcopilot:sha-64dc02fd055fda615e5b06351415b5e8d658a728
      credentials:
        username: ${{ github.actor }} 
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run pipeline
        run: node scripts/run-pipeline.js tests/fixtures/example-spec.json
